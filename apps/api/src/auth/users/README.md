# API User Foundation

This directory contains the minimal `User` persistence foundation for Slice 0.2.

It includes the locked user schema shape, schema indexes, repository foundation, and status helper service needed by later phone-first Auth tasks.

This directory does not contain Auth controllers, endpoints, OTP generation, SMS sending, password hashing, session persistence, RBAC, Profile, Admin, Content, Media, or product feature logic.

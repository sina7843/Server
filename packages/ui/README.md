# @dragon/ui

This package contains the shared Dragon Vue UI component package skeleton.

Slice 0.1 includes only a neutral placeholder component. It must not contain API calls or product-specific UI.

The package is Tailwind-ready at the package boundary, but it is not a full design system implementation yet.

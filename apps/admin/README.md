# Dragon Admin

This is the Dragon admin app shell.

It currently contains only a minimal Nuxt 3 shell. Admin is separate from the public web app, and it may use `ssr: false` in Phase 0.

No admin feature or product UI is implemented in Slice 0.1.

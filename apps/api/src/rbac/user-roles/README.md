# RBAC User Roles Foundation

This directory contains the Slice 0.3 UserRole persistence foundation.

UserRole records support optional `scopeType` and `scopeId` storage for later scoped authorization work. This task does not assign default roles, implement scoped authorization behavior, create role assignment APIs, or add Admin features.

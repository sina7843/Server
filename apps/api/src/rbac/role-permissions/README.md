# RBAC Role Permissions Foundation

This directory contains the Slice 0.3 RolePermission persistence foundation.

RolePermission records connect roles to system-defined permissions. This task does not seed mappings, implement permission guards, expose controllers, or add Admin RBAC APIs.

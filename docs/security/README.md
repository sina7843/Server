# Security

Security docs are placeholders in Slice 0.1.

No Auth, RBAC, or security control implementation exists yet. No secrets should be committed. `.env.example` files are examples only, and real `.env` files must remain local and ignored.
